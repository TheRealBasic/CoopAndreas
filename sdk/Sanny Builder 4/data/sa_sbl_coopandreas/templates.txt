cleo=CLEO script
={$CLEO .cs}
=nop
=
=|
=
=terminate_this_custom_script

load=model request and load
=request_model #|
=
=while not has_model_loaded #
=  wait 0
=end

def=Check if player is playing
=if
=  is_player_playing 0
=then
=
=end


scm=Multifile template (SA)
=DEFINE MISSIONS 0
=//DEFINE MISSION {ID} 0 AT {LABEL} @
=DEFINE EXTERNAL_SCRIPTS 0 // Use -1 in order not to compile AAA script
=//DEFINE SCRIPT {NAME}  AT {LABEL} @
=DEFINE UNKNOWN_EMPTY_SEGMENT 0
=DEFINE UNKNOWN_THREADS_MEMORY 0
=
=|
=
=while true
=  wait 250
=end

holdbtn=Hold ingame button template
=if // Insert the number of the ingame button to hold below
=	is_button_pressed 0 |
=then
=	if // Insert the intended milliseconds threshold below
=		timera > 
=	then
=		
=	end
=else
=	timera = 0
=end // if

holdkbd=Hold keyboard button template
=if // Insert the number of the keyboard button to hold below
=	is_key_pressed |
=then
=	if // Insert the intended milliseconds threshold below
=		timera > 
=	then
=		
=	end
=else
=	timera = 0
=end // if

forn=Iterate network players (CoopAndreas)
=for $temp_int = 0 to 2
=    if 
=        Coop.IsNetworkPlayerActorValid($NETWORK_PLAYER[$temp_int])
=    then
=        |
=    end
=end

missionboot=Mission bootstrap scaffold (CoopAndreas)
=script_name '{MISSION_NAME}'
=Coop.EnableSyncingThisScript()
=
=$NETWORK_PLAYER[0], $NETWORK_PLAYER[1], $NETWORK_PLAYER[2] = Coop.CollectNetworkPlayersForTheMission()
=
=// Canonical guarded remote actor operations pattern
=for $temp_int = 0 to 2
=    if
=        Coop.IsNetworkPlayerActorValid($NETWORK_PLAYER[$temp_int])
=    then
=        | // Coop.PrintNowForNetworkPlayer(..., $NETWORK_PLAYER[$temp_int])
=    end
=end
=
=// Wait until mission-critical entities get valid network IDs (-1 means not ready yet)
=$temp_int = Coop.GetPedNetworkId({MISSION_PED})
=while $temp_int == -1
=    wait 0
=    $temp_int = Coop.GetPedNetworkId({MISSION_PED})
=end
=
=$temp_int = Coop.GetVehicleNetworkId({MISSION_VEHICLE})
=while $temp_int == -1
=    wait 0
=    $temp_int = Coop.GetVehicleNetworkId({MISSION_VEHICLE})
=end
